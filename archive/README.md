# Archive

Superseded versions of individual files are moved here as the program advances,
following the versioning convention in the root README. Each archived file retains
its version tag so that the development of definitions, lemmas, and the draft can be
traced over time.

This directory is currently empty; it will populate as `theory/`, `program/`, and
`article/` files are revised.
